# Gold Vertical Panel (ระบบสร้างวิดีโอและโพสต์ราคาทองคำ)

โปรเจกต์นี้สำหรับสร้างคลิปวิดีโอแนวตั้ง (Reels/Shorts) รายงานราคาทองคำประจำวันแบบอัตโนมัติ โดยดึงข้อมูลจากเว็บไซต์สมาคมค้าทองคำ และสามารถโพสต์ลง Facebook Page ได้ทันที

## ✨ ความสามารถหลัก

- **ดึงข้อมูล:** ดึงราคาทองคำล่าสุดจากเว็บไซต์สมาคมค้าทองคำ (Gold Traders Association)
- **สร้างวิดีโอ:** สร้างไฟล์วิดีโอความละเอียด 1080x1920 (แนวตั้ง) เหมาะสำหรับ TikTok, Instagram Reels, YouTube Shorts
- **โพสต์ Facebook:** ระบบสร้างโพสต์ข้อความ รูปภาพ และโพสต์ลง Facebook Page ได้อัตโนมัติ (ผ่าน Graph API)
- **พื้นหลัง:** สุ่มวิดีโอพื้นหลัง (Dynamic Backgrounds) ให้ไม่ซ้ำกันในแต่ละครั้ง
- **แจ้งเตือน:** แสดงการเปลี่ยนแปลงราคา (ขึ้น/ลง) เทียบกับครั้งก่อนหน้า

## 🛠️ ความต้องการระบบ

- Python 3.x
- อินเทอร์เน็ตสำหรับดึงข้อมูลและโพสต์

### ไลบรารีที่ต้องติดตั้ง

```bash
pip install playwright requests moviepy numpy pillow
```

และติดตั้ง Playwright browser:

```bash
playwright install
```

---

## 🚀 วิธีใช้งาน

### 1. ดึงราคาทองคำ (Scrape Data)
รันไฟล์ `getgold.py` เพื่อดึงข้อมูลล่าสุดมาเก็บไว้ในไฟล์ `gold_prices.json`

```bash
python getgold.py
```

### 2. สร้างวิดีโอ (Generate Video)
รันไฟล์ `app.py` เพื่อสร้างคลิปวิดีโอ

```bash
python app.py
```

ไฟล์วิดีโอจะถูกบันทึกที่ `out/output.mp4`

### 3. ระบบโพสต์ Facebook (Facebook Auto Post) 🆕
โปรเจกต์นี้รองรับการสร้างโพสต์และโพสต์อัตโนมัติ มีสคริปต์ให้เลือกใช้ตามความต้องการ:

*   **สร้างโพสต์ข้อความ:** `python facebook_post.py`
*   **สร้างรูปภาพ:** `python facebook_image_post.py`
*   **โพสต์อัตโนมัติ:** `python facebook_auto_post.py`
*   **ครบวงจร (แนะนำ):** `python facebook_post_all_in_one.py`

#### การตั้งค่า Facebook API
1.  เปิดไฟล์ `facebook_config.json`
2.  ใส่ `page_id` และ `access_token` ของเพจที่คุณต้องการโพสต์
    *   *ดูวิธีขอ Access Token อย่างละเอียดในไฟล์ `FACEBOOK_POST_README.md`*

---

## 📂 โครงสร้างไฟล์

*   `app.py`: สคริปต์หลักสำหรับสร้างวิดีโอ
*   `getgold.py`: สคริปต์ดึงข้อมูลราคาทองคำ
*   `gold_prices.json`: ไฟล์เก็บข้อมูลราคาทองคำ
*   `facebook_post_all_in_one.py`: สคริปต์หลักสำหรับระบบ Facebook
*   `assets/`: โฟลเดอร์เก็บวิดีโอพื้นหลัง (ถูกแยกออกจาก Git เพื่อลดขนาด)
*   `out/`: โฟลเดอร์เก็บไฟล์ผลลัพธ์ (วิดีโอ, รูปภาพ, ไฟล์ข้อความ)

## ⚠️ หมายเหตุสำหรับนักพัฒนา

*   **ไฟล์ขนาดใหญ่:** ไฟล์วิดีโอในโฟลเดอร์ `assets/*.mp4` ถูกตั้งค่าให้ละเว้นใน Git (.gitignore) เพื่อป้องกันปัญหาพื้นที่จัดเก็บ หากต้องการเพิ่มพื้นหลังใหม่ ให้วางไฟล์ .mp4 ไว้ในโฟลเดอร์นี้ได้เลย
*   **Database:** ไฟล์ `gold_tracker.db` ใช้เก็บประวัติราคาเพื่อคำนวณการเปลี่ยนแปลง

---

**พัฒนาโดย:** กานต์ (KarnDIY)
